_   = require('underscore')
S   = require('string')

module.exports = function($) {
    title = null;

    title = $("title").text() || null

    if(title)
        title = S(title).trim().s

    return title
}